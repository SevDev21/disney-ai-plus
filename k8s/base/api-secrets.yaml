apiVersion: v1
kind: Secret
metadata:
  name: api-secrets
  namespace: disney-ai-plus
  labels:
    app.kubernetes.io/name: api
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: disney-ai-plus
type: Opaque
stringData:
  # NOTE: Replace these values with secure credentials in production
  # Use sealed-secrets or external-secrets-operator for secure secret management
  DATABASE_URL: "postgresql://disney_app:CHANGE_ME_IN_PRODUCTION@db:5432/disney_ai_plus"
  JWT_SECRET: "CHANGE_ME_IN_PRODUCTION"
  SUPABASE_URL: "CHANGE_ME_IN_PRODUCTION"
  SUPABASE_ANON_KEY: "CHANGE_ME_IN_PRODUCTION"
